name: flutter_application_1
description: "KHARCHA - Premium Expense Tracker"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  cloud_firestore: ^6.1.2
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  intl: ^0.20.2
  fl_chart: ^0.70.2
  shimmer: ^3.0.0
  workmanager: ^0.9.0+3
  flutter_local_notifications: ^19.4.0
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  pdf: ^3.11.3
  path_provider: ^2.1.5
  share_plus: ^10.1.3
  csv: ^6.0.0
  sqflite: ^2.3.5
  connectivity_plus: ^6.0.5
  permission_handler: ^11.3.1
  flutter_sms_inbox: ^1.0.4
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
