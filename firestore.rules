rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to expenses collection without authentication
    match /expenses/{expenseId} {
      allow read, write: if true;
    }
  }
}
